import { useRouterRecoil } from "src/hooks/useRecoil"
import { useSwr } from "src/hooks/useSwr"
import { useSoundPlay } from "src/hooks/useSoundPlay"
import { useTimer } from "src/hooks/useTimer"
import { useAuth } from "src/hooks/useAuth"
import { useKeyEvent } from "src/hooks/useKeyEvent"
import { useConfirm } from "src/hooks/useConfirm"
import { useLoadAndAuth } from "src/hooks/useLoadAndAuth"

/**
 * hooks export
 */

export {
  useRouterRecoil,
  useSwr,
  useSoundPlay,
  useTimer,
  useAuth,
  useKeyEvent,
  useConfirm,
  useLoadAndAuth,
}
